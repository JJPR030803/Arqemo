# Phase 0 — 2026-03-14

## Done this session

### Refactor: `cache_dir` moved out of `template.rs`
- `cache_dir()` renamed to `search_cache_dir()` and moved to `cache/mod.rs`
- `template.rs` now only owns `templates_dir()` — no cache concerns

### `arqemo init` implemented
- `config/error.rs` — added `CreateFailed { path, source }` variant
- `config/root.rs` — added `ConfigRoot::ensure()` (creates `~/.config/arqemo/` + `themes/`, mirrors `CacheLayout::ensure()` pattern)
- `lib.rs` — added `pub fn init()` orchestrating config + templates + cache creation, prints each resolved path
- `arqemo-cli/src/main.rs` — added `Init` subcommand wired to `arqemo_core::init()`

`arqemo init` is now idempotent and creates:
- `~/.config/arqemo/`
- `~/.config/arqemo/themes/`
- `~/.config/arqemo/templates/`
- `~/.cache/arqemo/`
- `~/.cache/arqemo/rendered/`
- `~/.cache/arqemo/themes/`

## Open items (carried forward)

- [ ] **Extract `apply.rs` module** — orchestration stubs still live in `lib.rs`
- [ ] **`brutalist/theme.toml` fixture** — needed for end-to-end apply test
- [ ] **`arqemo-ipc` basic hyprctl calls** — gaps, rounding, border color
- [ ] **Wire up apply pipeline end-to-end** — load → validate → render → ipc → wallpaper
- [ ] **`cache_tester.rs` integration test** — currently a stub, needs real assertions
