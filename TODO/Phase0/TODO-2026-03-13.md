# Phase 0 — 2026-03-13

Workspace scaffolded. Code quality pipeline passes (build, fmt, clippy, test, --help).

## Open items

### Must do (Phase 0 scope)

- [ ] **Wallpaper validation incomplete** — `validate_wallpaper()` only checks
      forbidden keys for `image` mode. `solid`, `glsl`, and `renderer` modes
      need their forbidden key checks too (see `.claude/schema.md` validation table).

- [ ] **Extract `apply.rs` module** — conventions.md says `apply.rs` owns pipeline
      orchestration. Currently the `apply()`, `validate()`, `list()` stubs live
      directly in `lib.rs`. Move orchestration into its own module.

- [ ] **`brutalist/theme.toml` fixture** — Phase 0 scope requires a working
      TOML-only theme with static wallpaper image. Create it in
      `crates/arqemo-core/tests/fixtures/` and/or `~/.config/arqemo/themes/brutalist/`.

- [ ] **`arqemo-ipc` basic hyprctl calls** — Phase 0 scope says basic socket calls
      for gaps, rounding, border color. Currently an empty stub.

- [ ] **Wire up apply pipeline end-to-end** — load theme.toml, validate, render
      templates, dispatch hyprctl calls, activate wallpaper.

### Noted but not blocking

- [ ] **`pedantic` priority** — `Cargo.toml` uses `{ level = "warn", priority = -1 }`
      which is correct, but `.claude/conventions.md` still shows `pedantic = "warn"`.
      Update the doc when convenient.

- [ ] **`rustfmt.toml` reduced** — `imports_granularity` and `group_imports` removed
      because they're nightly-only. Add them back if/when switching to nightly toolchain.

- [ ] **One-liner gate conflict** — conventions.md gate command uses `-D warnings`
      which promotes `todo!()` to errors. Either accept `todo!()` stubs fail the gate
      until they're replaced, or adjust the gate command.
